# Only needed when the shop is hosted somewhere other than the API — on Vercel,
# for instance, with the Python server on Render. Leave it unset when FastAPI
# serves the shop itself; requests then go to the same origin.
#
# No trailing slash.
VITE_API_BASE=https://your-shop.onrender.com

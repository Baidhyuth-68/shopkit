# Copy this file to ".env" and edit the values.
# NEVER commit the real .env file.

# ---------- Database ----------
# Hosted PostgreSQL - Neon
DATABASE_URL=postgresql+psycopg://user:password@ep-xxx.aws.neon.tech/neondb?sslmode=require

# ---------- Security ----------
# Generate with:
# python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=change-me-before-going-live
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ---------- First admin account (created on first run) ----------
ADMIN_EMAIL=baidhyuth68@gmail.com
ADMIN_PASSWORD=mcrmcr//
ADMIN_NAME=Admin

# ---------- Misc ----------
CORS_ORIGINS=*
SEED_DEMO_DATA=true
MAX_UPLOAD_MB=5

# ---------- API reference ----------
DOCS_PUBLIC=false